[project]
name = "mosquitovision-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools]
py-modules = ["main"]

[tool.uv]
override-dependencies = [
    "opencv-python-headless>=4.6.0",
]

[tool.fastapi]
entrypoint = "main:app"
